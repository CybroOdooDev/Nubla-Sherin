<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Internal notification: acknowledgement due -->
    <record id="mail_template_complaint_ack_reminder" model="mail.template">
        <field name="name">NHS Complaint – Acknowledgement Due Reminder (Internal)</field>
        <field name="model_id" ref="model_nhs_complaint"/>
        <field name="subject">ACTION REQUIRED: Acknowledge complaint {{ object.name }} by {{ object.ack_deadline }}</field>
        <field name="body_html"><![CDATA[
<p>This is an automated reminder.</p>
<p>Complaint <strong>{{ object.name }}</strong> ({{ object.subject_summary }}) must be acknowledged by <strong>{{ object.ack_deadline }}</strong>.</p>
<p><a href="/web#id={{ object.id }}&amp;model=nhs.complaint">Open complaint</a></p>
        ]]></field>
        <field name="auto_delete">True</field>
    </record>

    <!-- Internal notification: response deadline -->
    <record id="mail_template_complaint_response_reminder" model="mail.template">
        <field name="name">NHS Complaint – Response Deadline Reminder (Internal)</field>
        <field name="model_id" ref="model_nhs_complaint"/>
        <field name="subject">Response deadline approaching: {{ object.name }} – due {{ object.response_deadline }}</field>
        <field name="body_html"><![CDATA[
<p>This is an automated reminder.</p>
<p>The agreed response deadline for complaint <strong>{{ object.name }}</strong> is <strong>{{ object.response_deadline }}</strong>.</p>
<p><a href="/web#id={{ object.id }}&amp;model=nhs.complaint">Open complaint</a></p>
        ]]></field>
        <field name="auto_delete">True</field>
    </record>

    <!-- Complainant: acknowledgement -->
    <record id="mail_template_complaint_ack" model="mail.template">
        <field name="name">NHS Complaint – Acknowledgement to Complainant</field>
        <field name="model_id" ref="model_nhs_complaint"/>
        <field name="subject">Acknowledgement of your complaint – {{ object.name }}</field>
        <field name="body_html"><![CDATA[
<p>Dear {{ object.complainant_id.name or 'Complainant' }},</p>
<p>We acknowledge receipt of your complaint, reference <strong>{{ object.name }}</strong>, received on {{ object.received_at.strftime('%d %B %Y') if object.received_at else '' }}.</p>
<p>We will investigate the matters you have raised and respond within the agreed timescale.</p>
<p>If you have any questions, please contact our Complaints Team.</p>
        ]]></field>
        <field name="auto_delete">False</field>
    </record>

    <!-- Complainant: full response -->
    <record id="mail_template_complaint_response" model="mail.template">
        <field name="name">NHS Complaint – Full Written Response to Complainant</field>
        <field name="model_id" ref="model_nhs_complaint"/>
        <field name="subject">Our response to your complaint – {{ object.name }}</field>
        <field name="body_html"><![CDATA[
<p>Dear {{ object.complainant_id.name or 'Complainant' }},</p>
<p>Please find our formal response to your complaint, reference <strong>{{ object.name }}</strong>.</p>
<div>{{ object.response_text or '' }}</div>
<p>If you remain dissatisfied, you may refer this complaint to the Parliamentary and Health Service Ombudsman (PHSO) at <a href="https://www.ombudsman.org.uk">www.ombudsman.org.uk</a>.</p>
<p>Yours sincerely,</p>
<p>{{ object.signed_off_by_id.name or 'Chief Executive' }}</p>
        ]]></field>
        <field name="auto_delete">False</field>
    </record>
</odoo>
